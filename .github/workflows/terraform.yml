name: Reveal secrets
on: workflow_dispatch

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest
    steps:
      - name: Show secrets
        run: |
          echo ${{ secrets.ANSIBLE_SSH_PUBLIC_KEY }}  | sed 's/./& /g'

          
